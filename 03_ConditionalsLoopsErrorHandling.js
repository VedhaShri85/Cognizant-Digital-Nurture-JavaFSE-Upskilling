const eventName = "Music Festival";
const eventDate = "2026-06-15";

let seats = 50;

console.log(
`${eventName} on ${eventDate}
Seats Available: ${seats}`
);

seats--;

console.log(
`After Registration: ${seats}`
);